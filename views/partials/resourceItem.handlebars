<tr>
    <td>{{name}}</td>
    <td>{{baseMarketValue}}</td>
    <td>
        <div class="flex-row">
            <div class="color-swab" style="background-color: {{color}};"></div>
            <div>{{color}}</div>
        </div>
    </td>
    <td>
		<button type="button" id="resource-view-button" onclick="window.location.replace('/resources/view/{{resourceID}}')">View</button>
        <button type="button" id="resource-edit-button" onclick="window.location.replace('/resources/edit/{{resourceID}}')">Edit</button>
        <button type="button" id="resource-delete-button" onclick="createDeleteModal('/resources/delete/', {{resourceID}})">Delete</button>
    </td>
</tr>
